# ===========================================
# CONFIGURACIÓN DEL PROYECTO LAVADERO AL
# ===========================================

# Entorno (development, production)
ENV=development

# ===========================================
# CONFIGURACIÓN DE BASE DE DATOS
# ===========================================
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=lavadero_al
DB_PORT=3306

# ===========================================
# CONFIGURACIÓN DE SEGURIDAD JWT
# ===========================================
# IMPORTANTE: Genera una clave segura con:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# NUNCA uses una clave predecible en producción
SECRET_KEY=GENERA_UNA_CLAVE_SEGURA_AQUI

# Duración del token en minutos (por defecto 24 horas = 1440)
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ===========================================
# NOTAS IMPORTANTES
# ===========================================
# 1. NO subas este archivo con valores reales a Git
# 2. Copia este archivo como .env y configura tus valores
# 3. En producción, usa variables de entorno del sistema
# 4. Cambia SECRET_KEY regularmente por seguridad
